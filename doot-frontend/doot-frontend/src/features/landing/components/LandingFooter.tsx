export function LandingFooter() {
  return (
    <footer className="mx-auto flex max-w-[1180px] flex-col gap-2 border-t border-border-soft px-6 py-8 text-xs text-muted-foreground sm:flex-row sm:items-center sm:justify-between md:px-8">
      <span>DOOT — Distributed systems + cryptography simulation</span>
      <span>Built with React · TypeScript · Bluetooth Mesh (simulated)</span>
    </footer>
  );
}
